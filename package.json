{
  "name": "uid-bypass-dashboard",
  "version": "1.0.0",
  "description": "White-Label API & Reseller Command Suite",
  "main": "dist/server.js",
  "scripts": {
    "postinstall": "cd frontend && npm install",
    "build-frontend": "cd frontend && npm run build",
    "build-backend": "npx tsc",
    "build": "npm run build-frontend && npm run build-backend",
    "start": "node dist/server.js",
    "dev": "npx nodemon src/server.ts"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.9",
    "nodemon": "^3.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.2"
  }
}
